# ─── Database ────────────────────────────────────
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=sbom_cve

# ─── NVD API (optional but strongly recommended) ─
# Register free at: https://nvd.nist.gov/developers/request-an-api-key
# Without a key: 5 requests / 30 seconds
# With a key:   50 requests / 30 seconds
NVD_API_KEY=
